function _draw()
 --pset(x,y)
 --print("☉",x,y,x)
 --cls(
 
circfill(p_x+4,p_y+4,rnd(8)
 ,rnd(16))
 
 
 if (i_draw) then
  map(0,0,0,0,128,128)
  i_draw = false
  end
 

 spr(i_spr,i_x,i_y)
 spr(p_spr,p_x,p_y)
 
 
end
